# Web GUI for Clustering Systems

This directory contains the Flask backend and React frontend for managing clustering systems.

## Structure

- `app.py` - Main Flask application
- `api/` - API endpoints
- `frontend/` - React components
- `models/` - Data models
