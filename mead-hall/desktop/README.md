# Desktop GUI for Clustering Systems

This directory contains the Python desktop application for managing clustering systems.

## Structure

- `main.py` - Main application entry point
- `gui/` - GUI components
- `models/` - Data models
- `utils/` - Utility functions
