# Shared Components for Clustering Systems

This directory contains shared components between the web and desktop GUIs.

## Structure

- `models/` - Shared data models
- `utils/` - Shared utility functions
- `api_client.py` - Client for communicating with the Flask API
